server:
  port: 8082
spring:
  application:
    name: market-data
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
logging:
  level:
    com.trading.marketdata: INFO
